<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class=" block px-5 py-3 bg-zinc-800 w-full rounded-md " type="text"
                    placeholder="Input your title here" name="title">
                <textarea class=" block mt-5 px-5 py-3 bg-zinc-800 resize-none w-full rounded-md "
                    placeholder="Write down the details of your Task" name="details"></textarea>
                <input class="mt-5 bg-green-800 rounded-md px-3 py-2" type="submit" placeholder="Create Task">

            </form>
        </div>
        <div class="tasks p-10 flex flex-wrap ">
            <% if (files.length>0){ %>
                <% files.forEach(function(val){ %>
                    
                    <div class="task bg-zinc-800 min-w-72 rounded-md px-3 py-3 m-2 ">
                        <h1 class="text-white text-2xl tracking-tighter"><%= val%></h1>
                        <div class=" w-full flex justify-between items-center mt-3">
                            <a class="text-blue-700 inline-block " href="/file/<%=val %>"> Read More</a>
                            <a class="text-white bg-red-500 rounded-xl px-2 py-1" href="/delete/<%=val%>" onclick="return confirm('Are you sure you want to delete this file?');" >delete</a>
                            <a class="text-zinc-600" href="/edit/<%=val%>">edit</a>
                        </div>
                    </div>
                <%}) %>
            <%} else{ %>
                    <h2 class="text-gray-500">No tasks yet</h2>

            <% }%>
        </div>
    </div>


    <script src="/javascripts/script.js"></script>
</body>

</html>